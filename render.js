var renderListPost = function(data, element) {
    "use strict";
    var template = "";


    if (data.length === 0) {
        template = "";
    }


    _.forEach(data, function(value, key) {
        console.log();

    });
};

var renderListTodos = function(data) {

};